---
title: HallyuLatino 기여 가이드
version: 1.0.0
status: Draft
owner: @hallyulatino-team
created: 2025-11-25
updated: 2025-11-25
reviewers: []
language: Korean (한국어)
---

# CONTRIBUTING.md - 기여 가이드

HallyuLatino 프로젝트에 기여해 주셔서 감사합니다! 🎉

이 문서는 프로젝트에 기여하는 방법을 안내합니다.

## 변경 이력 (Changelog)

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|----------|
| 1.0.0 | 2025-11-25 | @hallyulatino-team | 최초 작성 |

## 관련 문서 (Related Documents)

- [CODE_REVIEW_GUIDE.md](./CODE_REVIEW_GUIDE.md) - 코드 리뷰 가이드
- [VERSIONING_GUIDE.md](./VERSIONING_GUIDE.md) - 버전 관리 가이드
- [docs/guides/CLEAN_CODE_GUIDE.md](./docs/guides/CLEAN_CODE_GUIDE.md) - 클린 코드 가이드

---

## 📋 목차

1. [행동 강령](#-행동-강령)
2. [기여 방법](#-기여-방법)
3. [개발 워크플로우](#-개발-워크플로우)
4. [브랜치 전략](#-브랜치-전략)
5. [커밋 컨벤션](#-커밋-컨벤션)
6. [Pull Request 가이드](#-pull-request-가이드)
7. [코드 스타일](#-코드-스타일)
8. [테스트 가이드](#-테스트-가이드)
9. [이슈 작성 가이드](#-이슈-작성-가이드)

---

## 🤝 행동 강령

### 우리의 약속

모든 기여자는 다음을 준수해야 합니다:

- **존중**: 다양한 의견과 경험을 존중합니다
- **포용**: 모든 배경을 가진 사람들을 환영합니다
- **건설적 피드백**: 비판은 건설적이고 구체적으로 합니다
- **협력**: 팀의 성공을 개인의 성공보다 우선합니다

### 금지 행위

- 모욕적이거나 차별적인 언어 사용
- 개인 공격이나 정치적 공격
- 스팸 또는 홍보성 콘텐츠
- 타인의 개인정보 무단 공개

---

## 🚀 기여 방법

### 기여 유형

| 유형 | 설명 | 난이도 |
|------|------|--------|
| 🐛 버그 리포트 | 버그 발견 시 이슈 등록 | 쉬움 |
| 📝 문서 개선 | 문서 오류 수정, 번역 | 쉬움 |
| 💡 기능 제안 | 새로운 기능 아이디어 | 보통 |
| 🔧 버그 수정 | 버그 수정 PR | 보통 |
| ✨ 기능 구현 | 새로운 기능 구현 | 어려움 |
| 🔒 보안 개선 | 보안 취약점 수정 | 어려움 |

### 첫 기여자를 위한 가이드

1. **Good First Issue** 라벨이 붙은 이슈를 찾아보세요
2. 이슈에 댓글을 달아 작업 의사를 표현하세요
3. 질문이 있으면 언제든 물어보세요

---

## 🔄 개발 워크플로우

### TDD (Test-Driven Development) 기반 워크플로우

```
┌─────────────────────────────────────────────────────────────┐
│                    TDD Development Cycle                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│    1. Issue 확인    2. 브랜치 생성    3. 테스트 작성 (RED)  │
│         │                │                  │               │
│         ▼                ▼                  ▼               │
│    ┌─────────┐     ┌─────────┐        ┌─────────┐          │
│    │ 요구사항 │────▶│ feature │───────▶│ 실패하는 │          │
│    │  분석   │     │ branch  │        │ 테스트  │          │
│    └─────────┘     └─────────┘        └─────────┘          │
│                                             │               │
│    6. PR 생성     5. 리팩토링 (REFACTOR)    │               │
│         │                │                  │               │
│         ▼                ▼                  ▼               │
│    ┌─────────┐     ┌─────────┐        ┌─────────┐          │
│    │   PR    │◀────│ 코드    │◀───────│ 구현    │          │
│    │  Review │     │ 개선    │        │ (GREEN) │          │
│    └─────────┘     └─────────┘        └─────────┘          │
│         │                                                   │
│         ▼                                                   │
│    ┌─────────┐                                             │
│    │  Merge  │                                             │
│    └─────────┘                                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 상세 단계

1. **Issue 확인**
   - 작업할 이슈를 선택합니다
   - 이슈에 자신을 할당합니다 (Assignee)

2. **브랜치 생성**
   ```bash
   git checkout main
   git pull origin main
   git checkout -b feature/issue-123-user-authentication
   ```

3. **테스트 작성 (RED)**
   - 실패하는 테스트를 먼저 작성합니다
   - 테스트가 실패하는지 확인합니다

4. **구현 (GREEN)**
   - 테스트를 통과하는 최소한의 코드를 작성합니다
   - 모든 테스트가 통과하는지 확인합니다

5. **리팩토링 (REFACTOR)**
   - 코드 품질을 개선합니다
   - 테스트가 여전히 통과하는지 확인합니다

6. **PR 생성**
   - 변경사항을 푸시하고 PR을 생성합니다
   - 리뷰어를 지정합니다

---

## 🌳 브랜치 전략

### 브랜치 구조

```
main (production)
  │
  ├── develop (integration)
  │     │
  │     ├── feature/issue-123-user-auth
  │     ├── feature/issue-456-video-player
  │     └── bugfix/issue-789-login-error
  │
  ├── release/v1.0.0
  │
  └── hotfix/critical-security-fix
```

### 브랜치 명명 규칙

| 브랜치 유형 | 패턴 | 예시 |
|-------------|------|------|
| 기능 | `feature/issue-{번호}-{설명}` | `feature/issue-123-user-auth` |
| 버그 수정 | `bugfix/issue-{번호}-{설명}` | `bugfix/issue-456-login-error` |
| 핫픽스 | `hotfix/{설명}` | `hotfix/security-patch` |
| 릴리스 | `release/v{버전}` | `release/v1.0.0` |
| 문서 | `docs/{설명}` | `docs/api-documentation` |

### 브랜치 규칙

1. **main 브랜치**
   - 항상 배포 가능한 상태 유지
   - 직접 커밋 금지 (PR만 허용)
   - 모든 테스트 통과 필수

2. **develop 브랜치**
   - 다음 릴리스를 위한 개발 통합
   - 기능 브랜치는 여기서 분기

3. **feature 브랜치**
   - develop에서 분기
   - 하나의 기능에 집중
   - 완료 후 develop으로 병합

---

## 📝 커밋 컨벤션

### Conventional Commits 형식

```
<type>(<scope>): <subject>

[optional body]

[optional footer]
```

### Type 종류

| Type | 설명 | 예시 |
|------|------|------|
| `feat` | 새로운 기능 추가 | `feat(auth): add JWT refresh token` |
| `fix` | 버그 수정 | `fix(api): resolve null pointer exception` |
| `docs` | 문서 변경 | `docs(readme): update installation guide` |
| `style` | 코드 포맷팅 (기능 변경 없음) | `style(lint): fix eslint warnings` |
| `refactor` | 리팩토링 | `refactor(user): extract validation logic` |
| `test` | 테스트 추가/수정 | `test(auth): add login failure tests` |
| `chore` | 빌드, 설정 변경 | `chore(deps): upgrade fastapi to 0.109` |
| `perf` | 성능 개선 | `perf(query): optimize user search` |
| `ci` | CI/CD 변경 | `ci(github): add security scan workflow` |

### Scope 종류

| Scope | 설명 |
|-------|------|
| `auth` | 인증 관련 |
| `user` | 사용자 관련 |
| `content` | 콘텐츠 관련 |
| `api` | API 관련 |
| `ui` | UI 컴포넌트 |
| `db` | 데이터베이스 |
| `deps` | 의존성 |
| `config` | 설정 |

### 커밋 메시지 예시

**좋은 예:**
```
feat(auth): add Google OAuth login

- Add OAuth2 flow for Google authentication
- Store OAuth tokens securely
- Create user account on first login

Closes #123
```

**나쁜 예:**
```
fixed stuff
```

### 커밋 규칙

1. **원자적 커밋**: 하나의 커밋은 하나의 논리적 변경만 포함
2. **현재형 사용**: "Add feature" (O), "Added feature" (X)
3. **제목 50자 이내**: 간결하게 작성
4. **본문 72자 줄바꿈**: 가독성을 위해

---

## 🔍 Pull Request 가이드

### PR 체크리스트

PR을 생성하기 전에 확인하세요:

- [ ] 관련 이슈를 링크했나요?
- [ ] 테스트를 추가/수정했나요?
- [ ] 모든 테스트가 통과하나요?
- [ ] 코드 스타일 검사를 통과했나요?
- [ ] 문서를 업데이트했나요?
- [ ] CHANGELOG에 변경사항을 추가했나요?

### PR 템플릿

```markdown
## 📋 변경 사항

<!-- 변경 내용을 설명해 주세요 -->

## 🎯 관련 이슈

<!-- 관련 이슈를 링크해 주세요 -->
Closes #123

## 🧪 테스트

<!-- 테스트 방법을 설명해 주세요 -->
- [ ] 단위 테스트 추가
- [ ] 통합 테스트 추가
- [ ] 수동 테스트 완료

## 📸 스크린샷 (UI 변경 시)

<!-- UI 변경이 있다면 스크린샷을 첨부해 주세요 -->

## ✅ 체크리스트

- [ ] 코드가 기존 스타일을 따릅니다
- [ ] 자체 코드 리뷰를 완료했습니다
- [ ] 코드에 주석을 추가했습니다 (복잡한 로직)
- [ ] 문서를 업데이트했습니다
- [ ] 변경사항이 새로운 경고를 발생시키지 않습니다
- [ ] 변경사항을 검증하는 테스트를 추가했습니다
```

### PR 리뷰 프로세스

1. **자동 검사**
   - CI 파이프라인 실행
   - 린트, 테스트, 보안 스캔

2. **코드 리뷰**
   - 최소 1명의 승인 필요
   - CODEOWNERS에 따른 자동 리뷰어 할당

3. **병합**
   - Squash and merge 사용
   - 브랜치 자동 삭제

---

## 🎨 코드 스타일

### Python (Backend)

```python
# ✅ 좋은 예
class UserAuthenticationService:
    """사용자 인증 서비스

    이메일/비밀번호 기반 인증과 OAuth 인증을 처리합니다.
    """

    def __init__(self, user_repository: UserRepository):
        self._user_repository = user_repository

    def authenticate(self, email: str, password: str) -> AuthResult:
        """사용자를 인증합니다.

        Args:
            email: 사용자 이메일
            password: 평문 비밀번호

        Returns:
            AuthResult: 인증 결과 (토큰 포함)

        Raises:
            InvalidCredentialsError: 인증 실패 시
        """
        user = self._user_repository.find_by_email(email)
        if not user or not user.verify_password(password):
            raise InvalidCredentialsError("Invalid credentials")
        return self._create_auth_result(user)


# ❌ 나쁜 예
class auth:
    def do_auth(self, e, p):
        u = self.repo.get(e)
        if u.check(p):
            return u.token()
```

### TypeScript (Frontend)

```typescript
// ✅ 좋은 예
interface UserProfile {
  id: string;
  email: string;
  nickname: string;
  avatarUrl?: string;
  createdAt: Date;
}

/**
 * 사용자 프로필을 조회합니다.
 *
 * @param userId - 사용자 ID
 * @returns 사용자 프로필 정보
 * @throws {NotFoundError} 사용자를 찾을 수 없는 경우
 */
async function getUserProfile(userId: string): Promise<UserProfile> {
  const response = await api.get(`/users/${userId}`);
  return response.data;
}


// ❌ 나쁜 예
async function getUser(id: any) {
  return await api.get('/users/' + id);
}
```

### 공통 규칙

| 항목 | 규칙 |
|------|------|
| 들여쓰기 | 2 spaces (Frontend), 4 spaces (Backend) |
| 줄 길이 | 최대 100자 |
| 함수 길이 | 최대 20줄 |
| 클래스 길이 | 최대 200줄 |
| 파일 길이 | 최대 400줄 |
| 네스팅 깊이 | 최대 3단계 |

---

## 🧪 테스트 가이드

### 테스트 구조

```
tests/
├── unit/                  # 단위 테스트
│   ├── auth/
│   │   ├── test_login.py
│   │   └── test_registration.py
│   └── content/
│       └── test_content_service.py
├── integration/           # 통합 테스트
│   ├── test_auth_flow.py
│   └── test_content_api.py
└── e2e/                   # E2E 테스트
    └── test_user_journey.py
```

### 테스트 명명 규칙

```python
# 패턴: test_should_{expected_behavior}_when_{condition}

def test_should_return_user_profile_when_valid_token_provided():
    """유효한 토큰이 제공되면 사용자 프로필을 반환해야 한다"""
    pass

def test_should_raise_error_when_invalid_email_format():
    """잘못된 이메일 형식이면 에러를 발생시켜야 한다"""
    pass
```

### 테스트 커버리지 목표

| 유형 | 목표 |
|------|------|
| Unit Test | 80% 이상 |
| Integration Test | 60% 이상 |
| E2E Test | Critical Path 100% |

### 테스트 실행

```bash
# 전체 테스트
make test

# 특정 테스트만
pytest tests/unit/auth/test_login.py

# 커버리지 포함
pytest --cov=app --cov-report=html
```

---

## 📮 이슈 작성 가이드

### 버그 리포트

```markdown
## 버그 설명

<!-- 버그에 대한 명확한 설명 -->

## 재현 단계

1. '...'로 이동
2. '....'를 클릭
3. '....'까지 스크롤
4. 에러 발생

## 예상 동작

<!-- 예상했던 동작 -->

## 실제 동작

<!-- 실제로 발생한 동작 -->

## 스크린샷

<!-- 해당되는 경우 스크린샷 첨부 -->

## 환경

- OS: [예: macOS 14.0]
- 브라우저: [예: Chrome 120]
- 버전: [예: 1.0.0]

## 추가 정보

<!-- 기타 관련 정보 -->
```

### 기능 요청

```markdown
## 기능 설명

<!-- 원하는 기능에 대한 명확한 설명 -->

## 문제/동기

<!-- 이 기능이 필요한 이유 -->

## 제안하는 해결책

<!-- 구현 방법에 대한 아이디어 -->

## 대안

<!-- 고려한 대안들 -->

## 추가 정보

<!-- 기타 관련 정보, 모형, 스크린샷 등 -->
```

### 이슈 라벨

| 라벨 | 설명 |
|------|------|
| `bug` | 버그 리포트 |
| `enhancement` | 기능 개선 |
| `feature` | 새로운 기능 |
| `documentation` | 문서 관련 |
| `good first issue` | 첫 기여자에게 적합 |
| `help wanted` | 도움 필요 |
| `priority: high` | 높은 우선순위 |
| `priority: low` | 낮은 우선순위 |

---

## 🙏 감사의 말

HallyuLatino에 기여해 주시는 모든 분들께 감사드립니다!

질문이 있으시면 언제든지 이슈를 통해 문의해 주세요.

---

*이 문서는 프로젝트의 성장에 따라 업데이트됩니다.*
